package com.example.optaplanner.frequencyPlanner;

import static org.junit.jupiter.api.Assertions.assertEquals;
import org.junit.Assert;
import org.junit.jupiter.api.Test;
import domain.FrequencyPlan;
import persistence.FrequencyPlanGenerator;

public class FrequencyPlanGeneratorTest {

	
	@Test
	public void createFrequencyPlanTest() {
		FrequencyPlanGenerator frPlanGenerator = new FrequencyPlanGenerator();
		
		//Assert.assertSame(Test.getDefaultInstance(), frPlanGenerator.createFrequencyPlan(10, 3, 4));
		
		//assertEquals(instanceof FrequencyPlan, frPlanGenerator.createFrequencyPlan(2, 3, 4));
		
	}
}
